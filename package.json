{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "concurrently \"nodemon server/index.js\" \"npm run dev --prefix client\"",
    "start": "node server/index.js",
    "build": "rimraf client/.next && npm install && npm install --prefix client --force && npm run build --prefix client"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.5.0",
    "express": "^5.1.0",
    "rimraf": "^6.0.1"
  },
  "devDependencies": {
    "@next/bundle-analyzer": "^15.4.2",
    "concurrently": "^9.1.2",
    "nodemon": "^3.1.10"
  }
}
