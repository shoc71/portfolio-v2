/** @type {import('next').NextConfig} */
const nextConfig = {
    // reactStrictMode: true,
    // swcMinify: true,
    // experimental: {
    //     turbo: true,
    //     serverActions: true
    // },
    // output: 'standalone',
};

export default nextConfig;
